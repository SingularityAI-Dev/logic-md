---
phase: 14-token-estimation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/compiler.ts
  - packages/core/compiler.test.ts
  - packages/core/types.ts
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02

must_haves:
  truths:
    - "estimateTokens('hello') returns approximately 2 (5 chars / 4 = 1.25, ceil to 2)"
    - "estimateTokens('') returns 0"
    - "A compiled step with a long systemPromptSegment (>8000 chars) includes a tokenWarning string"
    - "A compiled step with a short systemPromptSegment (<8000 chars) has no tokenWarning"
  artifacts:
    - path: "packages/core/compiler.ts"
      provides: "estimateTokens implementation and tokenWarning integration in compileStep"
      exports: ["estimateTokens"]
    - path: "packages/core/compiler.test.ts"
      provides: "Tests for estimateTokens and token warning in compileStep"
      contains: "estimateTokens"
    - path: "packages/core/types.ts"
      provides: "tokenWarning field on CompiledStep"
      contains: "tokenWarning"
  key_links:
    - from: "packages/core/compiler.ts"
      to: "packages/core/types.ts"
      via: "CompiledStep.tokenWarning field"
      pattern: "tokenWarning"
    - from: "packages/core/compiler.ts compileStep"
      to: "packages/core/compiler.ts estimateTokens"
      via: "inline call after building systemPromptSegment"
      pattern: "estimateTokens\\(systemPromptSegment\\)"
---

<objective>
Implement approximate token counting and prompt size warnings for compiled steps.

Purpose: Developers need to know when compiled prompt segments are approaching context window limits. A rough ~4 chars/token heuristic is sufficient for budget estimation without requiring a real tokenizer dependency.

Output: Working `estimateTokens()` function, `tokenWarning` field on `CompiledStep`, and tests proving both.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/compiler.ts
@packages/core/types.ts
@packages/core/compiler.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD estimateTokens and tokenWarning</name>
  <files>
    packages/core/types.ts
    packages/core/compiler.ts
    packages/core/compiler.test.ts
  </files>
  <action>
RED phase -- Write failing tests first:

1. In `compiler.test.ts`, import `estimateTokens` from `./compiler.js`.

2. Add a new `describe("estimateTokens")` block with these test cases:
   - `it("returns 0 for empty string")` -- `expect(estimateTokens("")).toBe(0)`
   - `it("estimates ~4 chars per token")` -- `expect(estimateTokens("hello world")).toBe(3)` (11 chars / 4 = 2.75, use `Math.ceil`)
   - `it("handles long text")` -- create a string of 8000 chars, expect `estimateTokens(str)` to return `2000`
   - `it("handles whitespace-only text")` -- `expect(estimateTokens("    ")).toBe(1)` (4 chars / 4 = 1)

3. Add a new `describe("token warnings")` block with these test cases:
   - `it("adds tokenWarning when segment exceeds 2000 tokens")` -- create a spec with a step whose `instructions` field is a string of 9000+ characters. Call `compileStep()` and assert `result.tokenWarning` is a string containing "2000" and "token".
   - `it("does not add tokenWarning for short segments")` -- use a spec with short instructions, assert `result.tokenWarning` is undefined or null.

4. Run tests -- all new tests MUST fail (estimateTokens throws "Not implemented", tokenWarning does not exist on type).

GREEN phase -- Make tests pass:

5. In `types.ts`, add `tokenWarning?: string` to the `CompiledStep` interface (after the `metadata` field).

6. In `compiler.ts`, replace the `estimateTokens` stub:
   ```typescript
   export function estimateTokens(text: string): number {
     if (text.length === 0) return 0;
     return Math.ceil(text.length / 4);
   }
   ```

7. In `compiler.ts` `compileStep()`, after building `systemPromptSegment` and before the `return` statement, add token warning logic:
   ```typescript
   const estimatedTokens = estimateTokens(systemPromptSegment);
   const tokenWarning = estimatedTokens > 2000
     ? `Prompt segment is ~${estimatedTokens} tokens (exceeds 2000 token budget)`
     : undefined;
   ```
   Add `tokenWarning` to the returned `CompiledStep` object.

8. Run tests -- all tests MUST pass, including existing tests (tokenWarning being undefined for short prompts should not break existing assertions since it is an optional field).

REFACTOR phase -- verify no cleanup needed (this is simple enough that refactoring is unlikely).
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run packages/core/compiler.test.ts</automated>
    <manual>Check that estimateTokens is exported from compiler.ts and tokenWarning appears on CompiledStep type</manual>
  </verify>
  <done>
    - estimateTokens("") returns 0
    - estimateTokens uses Math.ceil(text.length / 4) heuristic
    - compileStep adds tokenWarning string when segment exceeds 2000 tokens
    - compileStep omits tokenWarning when segment is under 2000 tokens
    - All existing compiler tests still pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/compiler.test.ts` passes with all new and existing tests
- `npx tsc --noEmit` passes (types are consistent)
- `estimateTokens` is importable from packages/core barrel export
</verification>

<success_criteria>
1. estimateTokens(text) returns Math.ceil(text.length / 4) for any input string, 0 for empty
2. CompiledStep includes optional tokenWarning field
3. compileStep produces tokenWarning when systemPromptSegment exceeds 2000 estimated tokens
4. All tests pass, zero type errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-token-estimation/14-01-SUMMARY.md`
</output>
